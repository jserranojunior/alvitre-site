version: "2"
services:
  php_nginx:
    build: .
    container_name: php_nginx
    working_dir: /app
    ports:
      - "80:80"
      - "8060:8060"
    volumes:
      - "../../site:/app"
      - "../../../acolher/front/dist:/app/acolher/front"
      - ../../../acolher/back:/app/acolher/back
