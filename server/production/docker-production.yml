version: "2"
services:
  apache:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.php_acolher.rule=Host(`alvitre.net`)"
      - "traefik.http.routers.php_acolher.entrypoints=web"
      - "traefik.http.services.php_acolher.loadbalancer.server.port=8081"
